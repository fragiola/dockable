import type { ExampleMeta } from "../meta-types";

export default {
    title: "Tabs at the bottom",
    description:
        "Tab strips below the content, by putting TabSetContent before the strip in the markup, with a toggle between top and bottom.",
    level: "basic",
    order: 8,
    features: ["markup order", "TabSetContent", "structural flex"],
    docs: "/docs/guides/styling-tailwind",
} satisfies ExampleMeta;
