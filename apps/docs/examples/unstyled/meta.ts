import type { ExampleMeta } from "../meta-types";

export default {
    title: "Unstyled",
    description:
        "The layout with no CSS at all: the primitives set only structural inline styles. Toggle the kit's class names on and off to see that the package paints nothing.",
    level: "basic",
    order: 2,
    features: ["structural styles", "no CSS", "className"],
    docs: "/docs/guides/styling-css",
} satisfies ExampleMeta;
