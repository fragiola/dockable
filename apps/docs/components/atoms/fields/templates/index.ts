// Vendored from the Fragiola UI registry (https://ui.fragiola.com/r/input.json, source registry/atoms/fields/templates/index.ts). Do not edit: re-run scripts/vendor-fragiola.ts.
import { TemplateSimple } from "./simple";

// Template namespace — access via Input.Template.Simple.
// One template per component today; namespace prepared for more.
const Template = {
    Simple: TemplateSimple,
};

export { Template };
